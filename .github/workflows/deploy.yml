name: Deploy to Ubuntu Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - working-directory: /var/www/essay_web
        run: |
          git pull origin main
          npm install
          npm run build
          pm2 restart essay-web